# tutetask17
### Map filter Exercise
  * showing student static dummy json data (array of objects) using <b> map()</b> method
  * search students name using input search box text by <b>filter()</b> method
  * showing on html page using <b>id<b/> and <b>.innerHTML</b>
